# QReader 全新用户界面设计方案

**设计角色**：UI Designer  
**项目**：QReader 电子书阅读器  
**目标**：打造一个美观、易用、沉浸、稳定、适合长时间阅读的全新界面体系  
**适用平台**：桌面 Web / Electron / Android WebView  
**设计日期**：2026-06-29

---

## 1. 设计定位

QReader 的核心不是“展示很多功能”，而是帮助用户稳定、舒适地完成三件事：

1. **找到书**：书库清晰、导入顺畅、最近阅读可快速回到现场。
2. **读下去**：阅读界面干净、低打扰、字体和主题调节自然。
3. **听下去**：朗读入口明确、状态可靠、段落跳转符合用户直觉。

因此，新 UI 建议采用：

> **Calm Reading OS / 安静阅读系统**

关键词：

- 沉浸式
- 纸感
- 克制高级
- 低视觉噪音
- 适合长文本
- 功能可发现但不打扰

---

## 2. 用户习惯与核心原则

### 2.1 用户习惯判断

电子书阅读器用户通常有以下行为习惯：

| 场景 | 用户习惯 | 设计响应 |
|---|---|---|
| 打开应用 | 希望快速继续上次阅读 | 首页首屏突出“继续阅读” |
| 管理书籍 | 希望看到封面、标题、进度 | 书库卡片显示封面、进度、最近阅读时间 |
| 阅读中 | 不想频繁看到复杂工具栏 | 默认轻量显示，点击唤醒控制栏 |
| 调字体 | 需要即时反馈 | 设置面板内提供实时预览区域 |
| 听书 | 需要明确播放、暂停、跳段状态 | 朗读面板独立成“播放器”心智 |
| PDF 标注 | 需要笔、荧光笔、橡皮快速切换 | 标注工具以浮动工具条呈现 |

### 2.2 UI 设计原则

1. **内容优先**：正文是最高层级，工具栏不能抢阅读注意力。
2. **一屏一个主要动作**：书库主动作是导入，阅读主动作是阅读/朗读。
3. **渐进披露**：高级设置放入底部面板或侧边面板，不一次性铺满。
4. **状态可见**：朗读、同步、解析、导入、保存都必须有明确反馈。
5. **触控友好**：移动端所有点击目标不低于 44px。
6. **无障碍优先**：颜色对比、键盘操作、焦点状态必须完整。

---

## 3. 信息架构

建议将 QReader 重新组织为 4 个主模块：

```text
QReader
├── 首页 Home
│   ├── 继续阅读
│   ├── 最近导入
│   ├── 快捷入口：导入 / 书库 / 设置
│   └── 阅读统计
│
├── 书库 Library
│   ├── 搜索
│   ├── 格式筛选
│   ├── 排序：最近阅读 / 最近导入 / 标题
│   ├── 书籍网格
│   └── 空状态导入引导
│
├── 阅读器 Reader
│   ├── 正文阅读区
│   ├── 顶部轻导航
│   ├── 底部功能栏
│   ├── 目录 / 书签 / 笔记 / 设置 / 朗读 面板
│   └── PDF 标注工具
│
└── 设置 Settings
    ├── 阅读偏好
    ├── 朗读偏好
    ├── 存储管理
    ├── 外观主题
    └── 关于与版本
```

---

## 4. 整体视觉方向

### 4.1 推荐风格

采用 **Soft Paper + Minimal Control** 风格：

- 背景偏暖白，不用纯白，降低长时间阅读疲劳。
- 卡片使用柔和阴影和浅边框，避免重装饰。
- 按钮圆角统一，交互状态明确但不过度跳动。
- 阅读页控制元素使用半透明磨砂层，唤起时出现，不打扰正文。

### 4.2 风格禁忌

避免以下问题：

- 不要使用过多渐变，阅读产品不适合过度炫技。
- 不要让阅读页像后台管理系统，按钮过多会打断沉浸感。
- 不要用 emoji 作为结构图标，应统一使用 SVG 图标。
- 不要让底部工具栏和正文抢层级。

---

## 5. 设计 Token 系统

### 5.1 色彩系统

#### Light Theme

```css
:root {
  --color-bg: #f7f3ea;
  --color-surface: #fffaf1;
  --color-surface-elevated: #ffffff;
  --color-text-primary: #1f2933;
  --color-text-secondary: #64748b;
  --color-text-muted: #94a3b8;

  --color-primary: #3b82f6;
  --color-primary-hover: #2563eb;
  --color-primary-soft: #dbeafe;

  --color-accent: #d97706;
  --color-accent-soft: #fef3c7;

  --color-success: #059669;
  --color-warning: #d97706;
  --color-error: #dc2626;

  --color-border: rgba(30, 41, 59, 0.12);
  --color-divider: rgba(30, 41, 59, 0.08);
}
```

#### Dark Theme

```css
[data-theme="dark"] {
  --color-bg: #111827;
  --color-surface: #1f2937;
  --color-surface-elevated: #273449;
  --color-text-primary: #f8fafc;
  --color-text-secondary: #cbd5e1;
  --color-text-muted: #94a3b8;

  --color-primary: #60a5fa;
  --color-primary-hover: #93c5fd;
  --color-primary-soft: rgba(96, 165, 250, 0.18);

  --color-accent: #fbbf24;
  --color-accent-soft: rgba(251, 191, 36, 0.16);

  --color-border: rgba(255, 255, 255, 0.12);
  --color-divider: rgba(255, 255, 255, 0.08);
}
```

### 5.2 字体系统

阅读器需要分离“界面字体”和“正文阅读字体”。

| 用途 | 推荐字体 | 说明 |
|---|---|---|
| UI 界面 | Inter / system-ui / PingFang SC / Microsoft YaHei | 清晰、现代、适合按钮和面板 |
| 中文正文 | 思源宋体 / Noto Serif SC / 系统宋体 | 长文可读性好 |
| 英文正文 | Georgia / Literata / Merriweather | 纸书感更强 |
| 等宽数据 | JetBrains Mono / SF Mono | 用于进度、调试、统计 |

#### 字号层级

```css
--font-size-xs: 12px;
--font-size-sm: 14px;
--font-size-base: 16px;
--font-size-lg: 18px;
--font-size-xl: 20px;
--font-size-2xl: 24px;
--font-size-3xl: 30px;
```

正文阅读建议：

- 默认字号：18px
- 行高：1.75
- 段落间距：0.9em
- 最大行宽：680px - 760px
- 中文段落不建议过窄，桌面端保持 32-42 个汉字/行较舒适。

### 5.3 间距系统

采用 4px 基础单位：

```css
--space-1: 4px;
--space-2: 8px;
--space-3: 12px;
--space-4: 16px;
--space-5: 20px;
--space-6: 24px;
--space-8: 32px;
--space-10: 40px;
--space-12: 48px;
```

使用建议：

- 按钮内边距：8px 14px / 10px 16px
- 卡片内边距：16px / 20px / 24px
- 面板区块间距：16px
- 页面主边距：移动端 16px，桌面端 24-32px

### 5.4 圆角与阴影

```css
--radius-sm: 6px;
--radius-md: 10px;
--radius-lg: 16px;
--radius-xl: 22px;
--radius-pill: 999px;

--shadow-sm: 0 1px 2px rgba(15, 23, 42, 0.06);
--shadow-md: 0 8px 24px rgba(15, 23, 42, 0.10);
--shadow-lg: 0 20px 48px rgba(15, 23, 42, 0.16);
```

阅读页控制层建议用 `--shadow-md`，弹出面板用 `--shadow-lg`。

---

## 6. 核心页面设计

## 6.1 首页 Home

### 页面目标

让用户 3 秒内知道：

- 上次读到哪本书
- 可以继续阅读
- 可以导入新书
- 书库里有什么最近内容

### 布局结构

```text
┌───────────────────────────────────────┐
│ 顶部：QReader Logo       搜索 / 设置   │
├───────────────────────────────────────┤
│ Hero：继续阅读                         │
│ ┌─────────────┐ 书名 / 作者 / 进度       │
│ │ 当前书封面   │ [继续阅读] [朗读]         │
│ └─────────────┘                         │
├───────────────────────────────────────┤
│ 快捷操作：导入书籍 / 打开书库 / 最近笔记 │
├───────────────────────────────────────┤
│ 最近阅读  横向卡片列表                  │
├───────────────────────────────────────┤
│ 阅读统计：本周阅读 / 总书籍 / 笔记数     │
└───────────────────────────────────────┘
```

### 设计要点

- “继续阅读”是首页唯一强主按钮。
- “导入书籍”作为次级高频入口，用描边或浅色按钮。
- 最近阅读卡片应显示进度条，不只显示封面。

---

## 6.2 书库 Library

### 页面目标

让用户快速找到书，并完成上传、筛选、排序、删除等管理动作。

### 布局结构

```text
┌───────────────────────────────────────┐
│ 标题：我的书库              [导入书籍] │
│ 搜索框：搜索书名、作者、格式            │
│ 筛选 Chips：全部 TXT PDF EPUB DOCX...  │
│ 排序：最近阅读 / 最近导入 / 标题        │
├───────────────────────────────────────┤
│ 书籍网格                               │
│ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│ │封面  │ │封面  │ │封面  │ │封面  │   │
│ │标题  │ │标题  │ │标题  │ │标题  │   │
│ │进度条│ │进度条│ │进度条│ │进度条│   │
│ └──────┘ └──────┘ └──────┘ └──────┘   │
└───────────────────────────────────────┘
```

### 组件细节

#### 书籍卡片

卡片内容顺序：

1. 封面
2. 格式 Badge
3. 标题
4. 作者 / 未知作者
5. 阅读进度
6. 最近阅读时间
7. 更多操作按钮

交互状态：

- Hover：卡片轻微上浮 2px，阴影增强。
- Focus：2px 蓝色焦点环。
- Delete：二次确认，不直接删除。

#### 空状态

没有书时不要只显示“暂无数据”，建议：

```text
这里还没有书
拖入 TXT / PDF / EPUB / MOBI / DOCX / Markdown 文件开始阅读
[导入第一本书]
```

---

## 6.3 阅读器 Reader

### 页面目标

正文是主角，工具在需要时出现。

### 布局结构

```text
┌───────────────────────────────────────┐
│ 顶部轻导航：返回 / 书名 / 章节 / 进度   │
├───────────────────────────────────────┤
│                                       │
│              正文阅读区域              │
│         max-width: 720px              │
│                                       │
├───────────────────────────────────────┤
│ 底部功能栏：目录 书签 笔记 朗读 设置    │
└───────────────────────────────────────┘
```

### 正文视觉

- 正文区域最大宽度：720px。
- 背景使用柔和纸色。
- 段落行高默认 1.75。
- 当前朗读段落使用左侧蓝色指示条 + 浅蓝背景，而不是大面积高亮。

### 顶部导航

应保持轻量：

- 左侧：返回
- 中间：书名 / 当前章节
- 右侧：搜索 / 全屏

滚动阅读时可自动弱化顶部导航，点击页面唤回。

### 底部功能栏

建议保留 5 个以内主入口：

1. 目录
2. 书签
3. 笔记
4. 朗读
5. 设置

当前页面已经接近这个结构，建议进一步统一：

- 图标统一使用同一套 SVG，线宽 1.75px 或 2px。
- 激活态使用蓝色背景胶囊。
- 朗读中底部朗读按钮增加脉冲小点，但动画要轻。

---

## 6.4 朗读面板 Read Aloud

### 设计目标

朗读面板应该像一个“小型播放器”，而不是普通设置表单。

### 推荐结构

```text
┌───────────────────────────────────────┐
│ 朗读                                  │
├───────────────────────────────────────┤
│ 状态卡片                              │
│  ┌───────────────┐                    │
│  │      播放按钮  │                    │
│  │ 正在朗读 / 已暂停 / 准备就绪        │
│  │ 当前段落提示                        │
│  └───────────────┘                    │
│                                       │
│ 音色                                  │
│ [晓晓 温柔] [晓依 自然] [云健 男声]    │
│                                       │
│ 语速                                  │
│ 慢  ━━━━━●━━━━━  快       1.0x        │
│                                       │
│ 行为提示                              │
│ 朗读中：点当前段落停止，点其他段落跳转  │
└───────────────────────────────────────┘
```

### 当前交互建议

根据用户已确认的规则：

| 状态 | 点击当前段落 | 点击其他段落 |
|---|---|---|
| 未朗读 | 不触发朗读 | 不触发朗读 |
| 朗读中 | 停止朗读，进入空闲 | 跳转到该段继续朗读 |
| 工具栏暂停中 | 恢复朗读 | 跳转到该段继续朗读 |
| 停止后 | 不响应 | 不响应 |

这个规则是合理的，因为“点击当前朗读段落”更像用户的明确停止意图，而不是临时暂停。

### 视觉状态

- 播放中：按钮为蓝色渐变 + 小波形动画。
- 暂停中：按钮变成播放图标，状态文案为“已暂停”。
- 错误：状态卡片边框变成红色，提示“朗读连接异常，请重试”。
- 代理异常：不要只在控制台显示，应在面板中给用户可见提示。

---

## 6.5 阅读设置面板

设置面板建议分为 4 组：

1. 字体
2. 字号 / 字重 / 行距
3. 主题
4. 阅读模式

### 推荐布局

```text
阅读设置
├── 字体
│   [系统默认] [宋体] [思源宋体] [霞鹜文楷]
├── 字号
│   [-] 18px [+]
├── 字重
│   [细] [常规] [中等] [粗] [特粗]
├── 行距
│   [紧凑] [舒适] [宽松]
├── 主题
│   [白天] [夜间] [护眼] [羊皮卷]
└── 预览
    一段示例文本实时展示效果
```

### 设计要点

- 设置项要即时生效。
- 字重、行距不建议只用数字，最好提供语义标签。
- 主题选择需要有小色块预览。

---

## 6.6 PDF 标注界面

PDF 阅读和普通文本阅读心智不同，建议为 PDF 提供独立浮动工具条。

### 工具条结构

```text
[选择] [画笔] [荧光笔] [橡皮] | 颜色 | 粗细 | 撤销
```

### 交互原则

- 当前工具必须明显高亮。
- 颜色不要只靠色块，悬浮或选中时显示名称。
- 橡皮擦模式必须有明显退出方式，避免用户误删。

---

## 7. 组件库规范

## 7.1 按钮 Button

### 类型

| 类型 | 用途 |
|---|---|
| Primary | 继续阅读、确认导入、开始朗读 |
| Secondary | 打开书库、取消、普通操作 |
| Ghost | 工具栏图标按钮 |
| Danger | 删除书籍、清空数据 |

### 尺寸

| 尺寸 | 高度 | 内边距 |
|---|---:|---|
| Small | 32px | 8px 12px |
| Medium | 40px | 10px 16px |
| Large | 48px | 12px 20px |

### 无障碍要求

- 所有 icon-only 按钮必须有 `aria-label`。
- 焦点态需要 2px outline。
- 禁用态不能只改变颜色，也要设置 disabled 属性。

---

## 7.2 面板 Bottom Sheet

底部面板是当前阅读器的重要交互形式，应统一规范：

```css
.bottom-panel {
  border-radius: 22px 22px 0 0;
  background: var(--color-surface-elevated);
  box-shadow: var(--shadow-lg);
  max-height: min(72dvh, 620px);
}
```

### 交互

- 点击关闭按钮关闭。
- 点击面板外可以关闭，但朗读中点击正文段落不应意外关闭。
- 移动端支持下滑关闭，但不要与正文滚动冲突。

---

## 7.3 Toast

Toast 用于短反馈，不承担复杂错误说明。

| 类型 | 时长 | 示例 |
|---|---:|---|
| 成功 | 2000ms | 已添加书签 |
| 普通 | 1500ms | 开始朗读 |
| 警告 | 3000ms | TTS 代理未连接 |
| 错误 | 手动关闭或 5000ms | 朗读失败，请重试 |

Toast 应使用 `aria-live="polite"`。

---

## 7.4 书籍卡片 BookCard

### 状态

- 默认
- Hover
- Focus
- 正在阅读
- 解析中
- 错误

### 卡片信息优先级

1. 封面
2. 标题
3. 作者
4. 阅读进度
5. 最近阅读时间
6. 更多操作

---

## 8. 响应式设计

### 断点

```css
--bp-mobile: 375px;
--bp-tablet: 768px;
--bp-desktop: 1024px;
--bp-wide: 1440px;
```

### 移动端

- 书库使用 2 列网格。
- 阅读页底部栏固定，按钮不超过 5 个。
- 面板高度不超过 75dvh。
- 所有触控目标不小于 44px。

### 平板

- 书库使用 3-4 列网格。
- 阅读器可显示左侧目录栏。
- 阅读正文 max-width 720px。

### 桌面

- 书库使用 5-6 列网格。
- 可启用侧边目录栏。
- 阅读设置和朗读面板可以从底部面板升级为右侧抽屉，但交互模式要统一。

---

## 9. 交互与动效

### 动效原则

- 微交互：150-200ms
- 面板进入：220-280ms
- 页面切换：200-300ms
- 使用 `transform` 和 `opacity`，避免动画 width/height。

### 推荐动效

| 场景 | 动效 |
|---|---|
| 面板打开 | translateY + opacity |
| 卡片 hover | translateY(-2px) + shadow |
| 播放按钮 | 轻微 scale press feedback |
| 朗读波形 | 低幅度循环，不超过 3 根柱明显跳动 |
| 主题切换 | 背景和文字颜色 200ms 过渡 |

### Reduced Motion

必须支持：

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 10. 无障碍标准

### WCAG AA 要求

- 正文与背景对比度 ≥ 4.5:1。
- 大字号标题对比度 ≥ 3:1。
- 图标按钮必须有文本标签或 `aria-label`。
- 焦点顺序应与视觉顺序一致。
- 所有面板打开后焦点应进入面板，关闭后回到触发按钮。

### 键盘操作

| 操作 | 行为 |
|---|---|
| Tab | 在可交互元素间移动 |
| Enter / Space | 激活按钮 |
| Esc | 关闭面板 / 弹窗 |
| ← / → | 翻页模式上一页 / 下一页 |
| Ctrl / Cmd + F | 搜索 |

---

## 11. 可实施的改版路线

### Phase 1：统一视觉基础

- 抽离 CSS Token。
- 统一按钮、卡片、面板、Toast 样式。
- 统一图标线宽和尺寸。
- 修复暗色模式对比度。

### Phase 2：重构书库与首页

- 首页增加继续阅读 Hero。
- 书库增加搜索、筛选、排序。
- 书籍卡片增加进度和状态。
- 优化空状态。

### Phase 3：优化阅读器

- 统一顶部导航和底部功能栏。
- 优化底部面板视觉。
- 设置面板增加实时预览。
- 强化朗读状态反馈。

### Phase 4：移动端与无障碍

- Android WebView 下优化 safe-area。
- 全部触控目标检查到 44px 以上。
- 键盘导航和 aria-label 补齐。
- Reduced Motion 支持。

---

## 12. 设计验收清单

### 视觉一致性

- [ ] 全局使用设计 Token，不在组件中随意写 raw hex。
- [ ] 所有按钮圆角、阴影、动效一致。
- [ ] 所有图标来自同一图标体系。
- [ ] Light / Dark / Green / Parchment 主题都有完整适配。

### 易用性

- [ ] 首页 3 秒内可继续阅读。
- [ ] 书库搜索、筛选、排序入口清晰。
- [ ] 阅读页默认不干扰正文。
- [ ] 朗读状态明确，错误可见。
- [ ] 设置调整即时反馈。

### 无障碍

- [ ] 文本对比度达到 WCAG AA。
- [ ] 键盘可以完成主要操作。
- [ ] icon-only 按钮有 aria-label。
- [ ] 面板焦点管理完整。
- [ ] 支持 Reduced Motion。

### 移动端

- [ ] 无横向滚动。
- [ ] 触控目标 ≥ 44px。
- [ ] 底部栏不遮挡正文。
- [ ] 面板不与系统手势冲突。
- [ ] 横屏仍可阅读和操作。

---

## 13. 总结

QReader 的新 UI 不应该追求“更花”，而应该追求“更安静、更可靠、更懂阅读”。

推荐方向是：

> **首页负责快速回到阅读，书库负责管理，阅读器负责沉浸，朗读面板负责清晰控制。**

只要围绕这个结构推进，QReader 会从“功能完整的阅读器”进一步升级为“体验成熟的阅读系统”。
